# 更新日志

## 2026-07-21
- 新增「自动更新」：小念现在能自己从发布源拉取最新版，你不用再重新下载整个压缩包。
  - 更新只动程序文件，绝不碰你的 `.env`（密钥）、`data/`（聊天记录等）、`venv/`（运行环境）。
  - 更新前自动备份、更新后自动编译校验，万一新版本有问题会一键回滚，绝不会把软件搞坏。
  - 控制台「软件更新」可手动检查；默认每次启动也会在后台静默检查并自动应用（重启后生效）。
  - 国内被墙时，在 `.env` 里填 `UPDATE_MIRROR=https://ghproxy.com/` 即可走镜像。
